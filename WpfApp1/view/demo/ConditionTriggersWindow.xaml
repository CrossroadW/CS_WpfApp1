<Window
    x:Class="WpfApp1.view.demo.ConditionTriggersWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp1.view.demo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ConditionTriggersWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.Resources>
        <Style x:Key="focusAndHover" TargetType="{x:Type Button}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderThickness" Value="5" />
                    <Setter Property="Foreground" Value="Black" />
                    <Setter Property="FontSize" Value="20" />
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True" />
                        <Condition Property="IsFocused" Value="True" />

                    </MultiTrigger.Conditions>
                    <Setter Property="Content" Value="is focus and hover" />
                    <Setter Property="FontSize" Value="30" />
                    <Setter Property="Foreground" Value="Green" />
                </MultiTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="opacityAnimation" TargetType="{x:Type Button}">
            <Style.Triggers>
                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                AutoReverse="True"
                                By="10"
                                Storyboard.TargetProperty="FontSize"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Window.Content>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button
                Width="200"
                Height="200"
                Content="普通Triggers效果"
                Style="{StaticResource focusAndHover}" />
            <Button
                Width="200"
                Height="200"
                Content="事件Triggers动画"
                Style="{StaticResource opacityAnimation}" />
        </StackPanel>
    </Window.Content>
</Window>
